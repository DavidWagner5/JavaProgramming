package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class AccessModifiers_Test3 {

    public static void main(String[] args) {

        //System.out.println(ProtectedAccessModifier.name1); // default is not visible outside the package;
        //System.out.println(ProtectedAccessModifier.name2); // always visible in the same package , protected is not always visible outside the package
        // , only if it is a subclass or an inheritance extension;
    }
}
